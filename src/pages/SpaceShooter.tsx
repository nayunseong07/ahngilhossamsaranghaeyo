import React from 'react';
import { Outlet } from 'react-router-dom';

const Layout = () => {
  return (
    <div>
      <h1>여기에 원하는 페이지 소스코드가 들어가 있어야 함.</h1>
    </div>
  );
};

export default Layout;